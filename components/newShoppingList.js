import React from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

const NewShoppingList = () => {
	return (
		<View>
			<Text>New shopping list</Text>
		</View>
	);
};

export default NewShoppingList;
